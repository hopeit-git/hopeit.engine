{
    "app" : {
      "name": "config-manager",
      "version": "${HOPEIT_APPS_API_VERSION}"
    },
    "engine": {
      "import_modules": ["hopeit.config_manager"]
    },
    "events": {
      "runtime_apps_config" : {
        "type": "GET",
        "plug_mode": "Standalone",
        "auth": ["Unsecured"]
      },
      "cluster_apps_config" : {
        "type": "GET",
        "plug_mode": "Standalone",
        "auth": ["Unsecured"]
      }
    }
  }
  